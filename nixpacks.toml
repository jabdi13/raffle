# Nixpacks configuration for Railway deployment
# Specifies Node.js version compatible with Prisma 7 (requires 20.19+ or 22.12+)

[variables]
NIXPACKS_NODE_VERSION = "20.19.5"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate"]

[start]
cmd = "npm run start:socket"
